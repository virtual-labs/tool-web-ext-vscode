# Update

- The code now triggers a workflow when the username and token is provided - the token must have workflow permissions (cannot be a fine-grained token)
- It is yet to return a failure or success
- Testing with a virtual-labs experiment repo is yet to be done
